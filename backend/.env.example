# Backend Configuration
PORT=8000
HOST=0.0.0.0

# CORS Settings
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Cache Directory
CACHE_DIR=./cache

# GitHub Repository
GITHUB_REPO_URL=https://raw.githubusercontent.com/doshea/nyt_crosswords/master

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/crossword

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# JWT — generate with: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=your-random-secret-key-at-least-32-chars
JWT_EXPIRY_HOURS=24

# Ably (real-time messaging for collaborative rooms)
ABLY_API_KEY=your-ably-api-key

# NYT Live Puzzles (optional — enables 2019-present puzzles)
# To get your NYT-S cookie:
#   1. Log in to nytimes.com with an active Games subscription
#   2. Open browser DevTools > Application > Cookies > nytimes.com
#   3. Copy the value of the "NYT-S" cookie
# The cookie expires every few months and must be manually refreshed.
NYT_COOKIE=
